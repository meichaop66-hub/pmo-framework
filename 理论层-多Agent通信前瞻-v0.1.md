# 理论层——多 Agent 通信前瞻（v0.1）

> **降级说明（2026-08-14）**：本文件原内联于《通用 PMO 骨架 v3.19》第九章。经方法架构师（伊伊 / 梅超）裁定（F1，骨架可读性反馈），理论层已从骨架核心正文移出为独立前瞻文件。降级不改变其"不上生产"的前瞻定位，也不改变骨架 v3.19 的版本号与身份。需要时单独阅读本文件即可；骨架正文第九章仅保留一句话定位 + 本链接。
> 
> 原内联说明（v3.11）：本理论层原为独立文件《PMO 多 Agent 通信理论层 v0.1》，v3.11 合并内联进骨架以消除"主文档引用独立文件"失效点。2026-08-14 又因骨架可读性（F1）重新降级为独立文件——印证 v3.11 预留的"视用户需求决定是否切分理论层独立产物"挂钩。

定位：前瞻性架构设计文档。描述当单会话集中式 PMO 无法承载时，正确的分布式多 Agent 架构应该长什么样。本层不上生产，只指导进化方向。

内联说明（v3.11）：本理论层原为独立文件《PMO 多 Agent 通信理论层 v0.1》。v3.11 将其合并内联进骨架文档，以消除"主文档引用独立文件"这一额外失效点，并使现实层迭代与理论层迭代可在同一遍编辑中完成、不再漂移。理论层概念上仍独立——保留其 v0.1 版本标识与"不上生产"定位；若需把它作为独立产物交付（如交给 AI-5 单独使用），可按本层 v0.1 版本标识从本文档切分出 standalone 文档。

与实战层的关系：实战层 = 本文档主体（《通用 PMO 骨架》主体，单会话集中式）。理论层（本文件）描述未来的架构。两者内联但独立迭代，各自诚实——实战层不假装能做到分布式，理论层不低估实现难度。

触发本层的生产条件（全部满足后才考虑从实战层升级）：

- 主项目支线数超过 8 条，单会话上下文管理出现明显瓶颈
- 有明确需要跨平台 AI 协作的场景（非同一平台的支线 AI）
- AI-5 学习路线完成（CrewAI + LangGraph + Docker 部署）
- 至少一个 Agent 能在云服务器上 7x24 运行自动接棒防御（v3.12 关联）：当本层进入实战、出现 AI-5 进程级 Agent 自动接棒时，自动接棒前必须先通过「接棒完整性自检」（见接任者必读 / 原则 P16）——目录章节比对、CHANGELOG+「文档结束」锚点、来源可信度。未通过则回退外部冷备份（默认 IMA），禁止基于截断 / 不可信材料接棒。这是 v3.12 缺口防御在理论层的前瞻落点：自动接棒比人工接棒更易吞下聊天残片，故完整性自检是它的最高优先级失败模式防线。

作者：伊伊（梅超）| 骨架 PMO 共同设计

生成时间：2026-08-11（初版 v0.1）| 降级独立：2026-08-14（骨架 v3.19 / F1）| 理论层版本：v0.1

### 目录（理论层内联）

- 第一章：实验记录——为什么文件通信失败了
- 第二章：正确架构——事件驱动的多 Agent 系统
- 第三章：技术路径——从 CrewAI 到 A2A
- 第四章：触发条件与迁移路径
- 附录：AI-5 学习路线（基础设施学习）

### 第一章：实验记录——为什么文件通信失败了

#### 实验设计

2026-08-10 至 2026-08-11，PMO 体系尝试了以下多 AI 协作架构：

骨架PMO（TRAE 会话 A）

│

├── 写入任务 → /workspace/ai_comm/ai6_tasks.md

│

├── 通知飞书知识库 → "任务邮箱"节点

│

└── 期望：AI-6（TRAE 会话 B）自动读取并执行

AI-6（TRAE 会话 B）

│

├── 写入产出 → /workspace/ai_comm/ai6_progress.md

│

├── 通知飞书知识库 → "上线通知"节点

│

└── 期望：骨架PMO自动感知并审阅

通信介质：/workspace/ai_comm/ 文件目录（共享工作区） + 飞书知识库"系统区/任务邮箱"

#### 实验结论：失败

根因：AI 会话是被动触发的进程，不会主动轮询任何外部存储。

不管通信介质是本地的 /workspace/ai_comm/ 还是云端的飞书知识库，本质都是被动存储器。它们存储数据，不触发动作。AI 会话不包含"定时检查文件是否更新"的轮询逻辑。每次任务交接必须有一个人类手动在两个会话间搬运信息。

#### 失败的架构假设

以下假设被验证为不成立：

|  |  |
|-|-|
| "AI 会话 A 写文件 → AI 会话 B 读文件"可以自动化 | AI 会话 B 不知道自己该去读文件。必须先有人类打开会话 B，说"去读 xxx 文件" |
| "飞书知识库可以作为通知机制" | 飞书知识库是文档存储，不是消息队列。写入文档不触发任何 AI 会话的事件 |
| "上线自检协议可以自动触达 PMO" | 自检协议只能在会话已被手动打开后执行。无法驱动会话打开 |
| "支线 AI 的数量可以线性扩展" | 每条支线 = 多一个需要手动搬运的会话。支线越多，人的通信负担越大——与"自动化"的初衷相反 |

#### 什么没有失败

以下在这个实验中被验证为成立的：

|  |  |
|-|-|
| 自包含启动包 | 不需要通信回路的部署包可以独立运转。WorkBuddy 实验证明了这一点 |
| 骨架 PMO 的抽象归纳能力 | 在单会话内，骨架 PMO 能有效地从实例反馈中提取可复用规则 |
| 行业模块体系 | 5 个挂载点（原则/任务/检查/归档/冲刺）在单会话内运转正常 |

### 第二章：正确架构——事件驱动的多 Agent 系统

#### 核心差异

文件通信的失败不是因为"文件"或"飞书"有问题——是因为架构选错了模式。

|  |  |  |  |
|-|-|-|-|
| 存储 | 文件系统（被动） | 消息队列（主动） |  |
| 触发方式 | 人类手动通知 | 事件自动推送 |  |
| Agent 形态 | AI 平台会话（被动） | 独立服务进程（主动） |  |
| 通信模型 | 轮询（无人执行） | 发布-订阅（自动） |  |
| 部署方式 | 依赖 AI 平台 | 云服务器 7x24 |  |

#### 目标架构

┌─────────────────┐

│   消息队列/总线   │

│  (RabbitMQ/Kafka) │

└───┬───┬───┬───┬──┘

│   │   │   │

┌─────────┘   │   │   └─────────┐

▼             ▼   ▼             ▼

┌──────────┐ ┌──────────┐ ┌──────────┐

│ PMO Agent │ │ 支线Agent│ │ 支线Agent│

│ (进程)    │ │ A (进程) │ │ B (进程) │

└──────────┘ └──────────┘ └──────────┘

│             │             │

└─────────────┴─────────────┘

│

共享状态存储

(Redis / DB)

关键特征：

- 每个 Agent 是一个独立运行的 Python 进程（不是 AI 平台会话）
- Agent 之间通过消息队列通信——发布任务、回传结果
- 人只需要和 PMO Agent 交互，PMO Agent 自动调度支线 Agent
- Agent 可以运行在不同的物理位置（本机、云服务器、不同平台）

#### A2A 协议（Google Agent-to-Agent Protocol）

Google 在 2025 年发布的 A2A 协议定义了 Agent 间通信的标准：

- Agent Card：每个 Agent 暴露一个 JSON 端点，描述自己的能力和接口
- JSON-RPC 2.0：标准化的远程调用协议
- Task 生命周期：任务创建 → 执行中 → 完成/失败 → 产物回传
- 安全层：OAuth 2.0 + ReBAC 权限控制A2A 解决的核心问题和 PMO 遇到的一致：不同平台、不同框架的 Agent 如何互操作。

#### 为什么这个架构目前不可行

实施事件驱动多 Agent 系统需要：

- 至少一个 Agent 能作为独立进程 7x24 运行在服务器上
- 基础设施（消息队列、状态存储、监控）搭建完成
- 运行者具备 Python + Docker + Linux 操作能力
- 稳定的 API 费用预算（每个 Agent 进程持续消耗 LLM API 调用）以上条件在 2026-08-11 均不满足。

### 第三章：技术路径——从 CrewAI 到 A2A

#### 分阶段路线

阶段1: Python基础（2-3周）

│  能看懂、能改、能跑 Python 脚本

▼

阶段2: CrewAI 实践（2周）

│  在本地跑通 3 个 Agent 的协作 demo

│  验证：多 Agent 在同一个进程中自动协作

▼

阶段3: LangGraph 实践（2-3周，可选）

│  理解 DAG 状态机 + Human-in-the-Loop

│  验证：工作流能持久化中断恢复

▼

阶段4: 部署与基础设施（1-2周）

│  Docker + 云服务器 + 环境变量管理

│  验证：Agent 在云上 7x24 运行

▼

阶段5: A2A 协议（2-4周，可选）

│  跨平台 Agent 通信

│  验证：不同平台的 Agent 通过标准协议协作

▼

生产就绪

#### 关键验证节点

|  |  |  |  |
|-|-|-|-|
| 阶段2完成 | 多 Agent 能在同一个 Python 进程中自动协作 | 3 个 Agent 协作完成一个任务，无需人类在中间搬运 |  |
| 阶段4完成 | Agent 能在服务器上 7x24 运行 | 部署到云服务器，持续运行 ≥ 72 小时无中断 |  |
| 阶段5完成 | 跨平台 Agent 能互操作 | 一个 CrewAI Agent 和一个 LangGraph Agent 通过 A2A 协议交换任务 |  |

### 第四章：触发条件与迁移路径

#### 何时从实战层升级

以下条件全部满足时，启动从实战层到理论层定义的架构升级：

- 业务规模触发：主项目支线数超过 8 条，单会话上下文管理出现明显瓶颈——PMO 每次打开会话需要超过 5 分钟才能恢复全量上下文
- 跨平台刚需：有至少 1 个场景明确需要在非 TRAE 平台运转的 AI 与其他 AI 实时协作（不是"贴启动包→断联运行"的离线模式，而是"实时通信"）
- 技术就绪：AI-5 学习路线阶段 1-4 全部完成，运行者具备以下能力：
- 能独立编写和调试 Python 脚本
- 能在本地跑通 CrewAI 多 Agent 协作 demo
- 能使用 Docker 容器化部署
- 能在云服务器上部署和维护服务
- 成本可接受：有稳定的 LLM API 预算支持多 Agent 持续运行

#### 升级不是替代，是补充

即使未来完成了迁移，实战层（单会话）的场景也不会被替代：

|  |  |  |  |
|-|-|-|-|
| 新项目快速启动 | 实战层（单会话） | 不需要基础设施，即开即用 |  |
| 自包含跨平台部署 | 实战层（部署包） | 离线模式，不需要通信回路 |  |
| 中小规模项目 | 实战层（单会话） | 单会话足够，多 Agent 是过度设计 |  |
| 大规模多 Agent 协作 | 理论层架构 | 只有这个场景需要分布式 |  |

### 附录：AI-5 学习路线（基础设施学习）

AI-5 是个人学习项目，目标是为未来的多 Agent 基础设施迁移储备技术能力。当前状态：待启动。 触发条件：用户明确说"开始 AI-5 学习"，或理论层的 4 个触发条件中有 ≥ 2 个接近满足。

（以下阶段内容与 v0.1 原文档一致，内联于此，便于随骨架一并迭代。）

#### 阶段 1：Python 基础（目标 2-3 周）

学习目标：能看懂、能改、能跑 Python 脚本（不需要能从零写）

|  |  |  |  |
|-|-|-|-|
| 1.1 语法基础 | 变量、数据类型、条件、循环、函数 | 能读懂一个 50 行的 Python 脚本 |  |
| 1.2 数据结构 | 字典、列表、元组、集合 | 能操作嵌套字典（JSON 解析场景） |  |
| 1.3 面向对象 | 类、实例、方法、继承 | 能看懂 CrewAI 的 Agent 类定义 |  |
| 1.4 异步编程 | async/await、协程、asyncio | 能理解 async def 和 await 的执行顺序 |  |
| 1.5 文件与路径 | open、with、pathlib、os.path | 能读写 JSON 配置文件 |  |
| 1.6 pip 与虚拟环境 | venv、pip install、requirements.txt | 能创建虚拟环境并安装 CrewAI |  |

#### 阶段 2：CrewAI 实践（目标 2 周）

学习目标：在本地跑通 3 个 Agent 的协作 demo

|  |  |  |  |
|-|-|-|-|
| 2.1 CrewAI 安装与 hello-world | 安装、第一个 Agent、第一个 Task | 跑通官方 quickstart |  |
| 2.2 多 Agent 协作 | Crew、Agent、Task、Process | 3 个 Agent 协作完成一个任务 |  |
| 2.3 角色定义与 Handoff | role、goal、backstory、handoff | 模拟 PMO 场景：PMO + 2 个支线 Agent |  |
| 2.4 工具集成 | SerperDevTool、自定义 Tool | Agent 能调用外部 API |  |
| 2.5 LLM 模型切换 | LiteLLM 路由、OpenAI/Claude/Ollama | 同一个 Crew 切换不同模型运行 |  |

#### 阶段 3：LangGraph 实践（目标 2-3 周，可选）

学习目标：理解 DAG 图状态机，能跑通一个带 Human-in-the-Loop 的工作流

|  |  |  |  |
|-|-|-|-|
| 3.1 LangGraph 基础 | StateGraph、Node、Edge、条件分支 | 跑通官方 hello-world |  |
| 3.2 状态管理 | State、Checkpointing、Memory | 工作流能持久化中断恢复 |  |
| 3.3 Human-in-the-Loop | interrupt_before、审批节点 | 工作流在某节点暂停等用户确认 |  |
| 3.4 LangSmith 调试 | Trace、Time-travel debugging | 能用 LangSmith 查看执行链路 |  |

#### 阶段 4：部署与基础设施（目标 1-2 周）

学习目标：能把多 Agent 系统部署到云服务器 7x24 运行

|  |  |  |  |
|-|-|-|-|
| 4.1 Docker 基础 | Dockerfile、docker-compose、镜像 | 能把 CrewAI demo 容器化 |  |
| 4.2 Linux 命令行 | SSH、进程管理、日志、文件操作 | 能 SSH 到远程服务器操作 |  |
| 4.3 云服务器部署 | 开 VM、Nginx、SSL | 多 Agent 系统在云上跑通 |  |
| 4.4 环境变量管理 | .env、不硬编码密钥 | API Key 通过环境变量注入 |  |

#### 阶段 5：A2A 协议（目标 2-4 周，可选，按需启动）

学习目标：让不同平台的 Agent 跨平台通信

|  |  |  |  |
|-|-|-|-|
| 5.1 A2A 协议概念 | Agent Card、JSON-RPC 2.0、Task 生命周期 | 能画出 A2A 通信流程图 |  |
| 5.2 A2A SDK 实践 | Python SDK、Agent 端点搭建 | 跑通 A2A hello-world（2 个 Agent 互通） |  |
| 5.3 安全与认证 | OAuth 2.0、Scope、ReBAC 基础 | 能配置基本认证 |  |
| 5.4 跨平台集成 | 让 CrewAI Agent 通过 A2A 通信 | 2 个不同平台的 Agent 协作完成任务 |  |

#### 教学风格（AI-5 启动时使用）

- 目标导向：每个模块先说"学完能做什么"，再说"怎么学"
- 练习驱动：每个模块必须有一个动手练习
- PMO 场景关联：练习尽量用 PMO 相关场景
- 诚实评估：掌握就是掌握，没掌握就是没掌握，不敷衍理论层 v0.1（v3.11 内联骨架；2026-08-14 经 F1 降级为独立文件）| 初版 2026-08-11 | 骨架 PMO